import { AppRegistry } from 'react-native';
import App from './'; // Asegúrate de que esta ruta sea correcta
import { name as LCam } from './app.json';

AppRegistry.registerComponent(LCam, () => App);
